version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - echo 'test auto when have commit'

workflows:
  main:
    jobs:
      - build-and-test
